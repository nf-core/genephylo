{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://raw.githubusercontent.com/nf-core/genephylo/master/assets/schema_input.json",
    "title": "nf-core/genephylo pipeline - params.input schema",
    "description": "Schema for the file provided with params.input",
    "type": "array",
    "items": {
        "type": "object",
        "properties": {
            "id": {
                "type": "string",
                "pattern": "^\\S+$",
                "errorMessage": "Gene name must be provided and cannot contain spaces",
                "meta": ["id"]
            },
            "fastafile": {
                "type": "string",
                "format": "file-path",
                "exists": true,
                "pattern": "^\\S+\\.fa(sta?|s)?$",
                "errorMessage": "Fasta file containing at least 1 sequence must be provided, cannot contain spaces and must have extension '.fa', '.fas' or '.fasta'"
            },
            "treefile": {
                "type": "string",
                "format": "file-path",
                "exists": true,
                "pattern": "^\\S+\\.txt$",
                "errorMessage": "Phylogenetic tree file cannot contain spaces and must have extension '.txt'"
            }
        },
        "required": ["id", "fastafile"]
    }
}
